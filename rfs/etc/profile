export LANG="C.UTF-8"

if [ "`id -u`" -eq 0 ]; then
  PATH="/usr/sbin:/usr/bin:/sbin:/bin"
else
  PATH="/usr/bin:/bin"
fi
export PATH

if [ -d /etc/profile.d ]; then
  for i in /etc/profile.d/*.sh; do
    if [ -r $i ]; then
      . $i
    fi
  done
  unset i
fi
